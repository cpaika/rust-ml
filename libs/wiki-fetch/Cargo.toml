[package]
name = "wiki-fetch"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
ureq = { version = "3.0", features = ["json"] }
regex = "1.11"

[dev-dependencies]
tempfile = "3.14"
